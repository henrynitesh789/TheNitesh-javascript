"use strict";  // treat all JS code as newer version
//alert("welcom")  // we are using nodejs, not browser

let name = "nitesh"
let age = 18
let mature = false
let state = null

/* PRIMITIVE DATA TYPES

    number  -> 2 to power 53
    biginit -> bigger integer
    string  -> use in double quotes (prefered)
    boolean -> true/false
    null    -> standalone value
    undefined ->
    symbol -> unique */

/* NON-PRIMITIVE DATA TYPES
    object     */

console.log(typeof "nitesh");
console.log("age");
console.log(typeof null);
console.log(typeof undefined);
